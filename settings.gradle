pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
      id 'io.quarkus' version "${quarkusPluginVersion}"
      id 'io.jsonwebtoken' version "${jwtVersion}"
    }
}
rootProject.name='hello-world'